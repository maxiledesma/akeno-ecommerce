import NavBar from "./components/NavBar";
import CartWidget from "./CartWidget";

function Header() {
    return <header>
        <NavBar/>
        <CartWidget/>
    </header>
}

export default Header;
